# DEPLOY.md — เอาแอปขึ้นออนไลน์ ฟรี 100%

## ภาพรวมต้นทุน

| รายการ | ค่าใช้จ่าย |
|---|---|
| โฮสต์เว็บ (GitHub Pages) | ฟรี |
| HTTPS + URL (`ชื่อคุณ.github.io/ora`) | ฟรี |
| AI (Gemini API free tier) | ฟรี (ผู้ใช้แต่ละคนใช้ key ของตัวเอง) |
| PWA ติดตั้งลงมือถือได้เหมือนแอปจริง | ฟรี (ไม่ต้องจ่าย App Store $99/ปี, Play Store $25) |
| **รวม** | **0 บาท** |
| (ทางเลือก) โดเมนเอง เช่น ora.app | ~400–600 บาท/ปี — ยังไม่จำเป็นตอนนี้ |

## ขั้นตอน Deploy ครั้งแรก (~15 นาที ทำครั้งเดียว)

1. สร้าง repo บน GitHub (ตั้งชื่อ เช่น `ora`) — ถ้ายังไม่มีบัญชี สมัครฟรีที่ github.com
2. ใน Claude Code สั่ง:
   ```
   ช่วย init git ในโฟลเดอร์นี้ commit ทุกไฟล์ แล้ว push ขึ้น repo <ลิงก์ repo ของฉัน>
   ```
3. บน GitHub: **Settings → Pages → Source: Deploy from a branch → Branch: main, Folder: /app**
   (ถ้าเลือกได้เฉพาะ root ให้บอก Claude Code ว่า "ย้าย app ขึ้น root หรือทำ GitHub Action deploy เฉพาะโฟลเดอร์ app")
4. รอ 1–2 นาที → แอปออนไลน์ที่ `https://<username>.github.io/ora/`
5. เปิดจากมือถือ → เบราว์เซอร์จะเสนอ "เพิ่มไปยังหน้าจอโฮม" → ได้แอปติดเครื่องเหมือนโหลดจากสโตร์

## อัปเดตเวอร์ชันหลังแก้โค้ด

สั่ง Claude Code:
```
รัน test ให้ผ่านก่อน แล้ว commit + push การแก้ไขล่าสุด
```
GitHub Pages อัปเดตอัตโนมัติใน 1–2 นาที (ผู้ใช้เก่าอาจต้องปิด-เปิดแอป 1 ครั้งเพราะ service worker cache)
**สำคัญ:** ทุกครั้งที่แก้ไฟล์ใน `app/` ให้เปลี่ยนเลขเวอร์ชัน `CACHE = "ora-v1"` ใน `sw.js` (v2, v3, ...) เพื่อบังคับ cache ใหม่

## เปิดโหมด AI (Gemini key ฟรี)

1. ไปที่ https://aistudio.google.com/apikey (ล็อกอิน Google)
2. **Create API key** → copy
3. ในแอป: ตั้งค่า → วาง key → บันทึก
- Free tier พอสำหรับใช้ส่วนตัว/ทดสอบ (จำกัด request ต่อวัน — เช็คโควตาปัจจุบันใน AI Studio)
- ระยะ beta แนะนำให้ผู้ทดสอบแต่ละคนขอ key ฟรีของตัวเอง (แอปมีลิงก์บอกวิธีอยู่แล้ว) = คุณไม่มี cost และไม่ต้องมี server
- เมื่อมีผู้ใช้จริงจำนวนมากค่อยยกระดับ: Cloudflare Worker ฟรี 100k req/วัน เป็น proxy ถือ key กลาง — ค่อยทำตอนนั้น (สั่ง Claude Code สร้างให้ได้)

## เช็คลิสต์ก่อนปล่อยให้คนอื่นใช้

- [ ] `node tests/test-engine.js` → ALL PASSED
- [ ] ทดสอบบนมือถือจริง 1 เครื่อง (เปิดผ่าน URL จริง ไม่ใช่ localhost)
- [ ] ทดสอบคำถามอ่อนไหว ("เลขเด็ด", ประโยคเชิงวิกฤต) → ระบบต้องเข้าโหมด supportive
- [ ] disclaimer แสดงครบ (onboarding, ตั้งค่า, ท้ายคำตอบ AI)
- [ ] ลองปุ่ม "ลบข้อมูลทั้งหมด" → ข้อมูลหายจริง
